spring:
  application:
    name: tests

  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  data:
    syncache:
      brokerUrl: "wss://broker.syncache.tabariyya.com/"
      brokerAuthToken: ${BROKER_TOKEN}
      maxNoOfEntries: 500

  cache:
    type: syncache